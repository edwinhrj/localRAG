{
  "name": "my-project",
  "version": "1.0.0",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\" \"npm run start-electron\"",
    "start-backend": "cd backend && poetry install --no-root && uvicorn main:app --reload --host localhost --port 8000",
    "start-frontend": "cd frontend && npm install && npm run dev",
    "start-electron": "npm install && wait-on http://localhost:5173 && electron ."
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "electron": "^15.5.7",
    "wait-on": "^8.0.3"
  }
}
